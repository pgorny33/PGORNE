# P.GÓRNE Accent-Only Homepage

This version keeps the animation visible on its final frame and overlays only the Pantone accent mark. It does not fade in a second logo, so the experience feels continuous.

Replace the existing GitHub files with these files. Vercel should redeploy automatically.
